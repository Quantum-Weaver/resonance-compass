# COMPASS — THE FEATURE BOARD
*Assembled 2026-07-19 (the workspace honoring). Standing: v2 complete
(19 phases + parity), v2.3.0 = the native engine (v3 Phase 1 CLOSED).
Detail lives in V3-BUILD-SEQUENCE.md and in the base — the realm's open items and plans live in the base — `python C:/_superposition/resonance-progenatrix/progenatrix.py recall --realm resonance-compass` (the checklist retired in KP's 2026-08-25 cleanup, under his ruling that no checklist docs exist); this is the one-look
board.*

## TIER A — nearest
- **v3 Phase 2: Recording** 🎙️ — device spike first (cpal mic in the
  Tauri Android process), then input select, level meter, takes, BT
  latency calibration.
- **v3 Phase 0: The Summons** — market/user-voice scan → V3-LANDSCAPE.md
  (parallel any time; shapes Phases 2–4).
- **The UX polish sitting** — B1 (EQ settings pop/shift) + B2 (Studio:
  silent sliders need "applies on export" or live preview; Play Mix
  below the fold).

## TIER B — the instruments
- **v3 Phase 3: Four-Track** — overdub, per-track gain/pan/mute, bounce
  on the engine, stems export.
- **v3 Phase 4: Musician's Tools** — tuner · visible-time metronome ·
  **lyric capture with chords above the exact syllable** · sheet/tab
  pad · setlist builder (energy/key/tempo/arc).

## TIER C — gated / later
- **v3 Phase 5: Sanctuary Connection** — collab, publish, The Studio,
  Ableton Link if feasible (gated on the weave/doorway).
- **The naming** — Musician's Compass vs Resonance Studio (Council/KP).
- **Play track update** — when the musician features are live (KP's law).
  *Beside, 2026-09-01: the closed track already carries compass — Console:
  Closed testing, Aug 30, 2026 (KP's paste 09-01) — so this row now names
  only the PRODUCTION/public listing decision, not the closed track.*

## Maintenance
- 59 hardcoded hex → cosmic tokens (heaviest in the family).
- The human-test column (17b→19) walked on device, unhurried.
- Export note: fragment/mix AUDIO files still outside JSON export
  (documented limitation; a zip-into-export command someday).
