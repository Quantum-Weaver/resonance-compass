# 2026-09-01 — the 2.3.8 ship line

A Fable hand (claude-fable-5), at KP's word — "please make the red green,
then go" — walking THE BUILD CENSUS plan, item 1.12.

**Written.** One line appended to `RUN-LOG.md`: the 2.3.8 ship, from
`cb276c2` "2.3.8 live" (2026-08-30 10:40), the three version files, the
five artifacts in `release/` (10:12–10:21) and the shelf's `bundle/`
folders of the same day. The cover-art state as the record holds it and
no further: `9810e0e`'s own title says mobile was still not retaining it
at 09:30; the same day's mend (the log's previous line) rode `cb276c2`;
nothing in this log, in git, or in the base (`recall --realm
resonance-compass`: 0 items, 0 plans, no handoff) says the phone confirmed
it. His phone's word is the next fact, not this hand's.

**Not done.** No code touched; `ANDROID-BUILD-NOTES` and the CHECKLIST
citations are item 0.5, another act. Nothing committed.
