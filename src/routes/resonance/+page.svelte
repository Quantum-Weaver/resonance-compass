<div class="placeholder" style="padding-top: env(safe-area-inset-top, 0px);">
	<header class="ph-header">
		<h1 class="ph-title">Resonance</h1>
	</header>
	<div class="ph-body">
		<p class="ph-icon">✨</p>
		<p class="ph-heading">Your emotional map</p>
		<p class="ph-sub">Mood patterns from your listening history. Coming in Phase 4.</p>
	</div>
</div>

<style>
	.placeholder { min-height: 100%; display: flex; flex-direction: column; }
	.ph-header { padding: 1rem 1.25rem 0.75rem; border-bottom: 1px solid var(--border-color); }
	.ph-title { font-size: 1.25rem; font-weight: 700; color: var(--text); margin: 0; }
	.ph-body { flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 0.75rem; padding: 3rem 2rem; text-align: center; }
	.ph-icon { font-size: 3rem; margin: 0; }
	.ph-heading { font-size: 1rem; font-weight: 600; color: var(--text); margin: 0; }
	.ph-sub { font-size: 0.875rem; color: var(--text-muted); margin: 0; }
</style>
